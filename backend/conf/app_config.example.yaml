logging:
  file:
    enable: true
    level: INFO
    path: logs
    rotation: "10 MB"
    retention: "7 days"
  console:
    enable: true
    level: INFO

db_meta:
  host: localhost
  port: 3307
  user: your_db_user
  password: your_db_password
  database: meta

db_dw:
  host: localhost
  port: 3307
  user: your_db_user
  password: your_db_password
  database: dw

qdrant:
  host: localhost
  port: 6333
  embedding_size: 1024

embedding:
  host: localhost
  port: 8081
  model: BAAI/bge-large-zh-v1.5
  api_key: ""  # HuggingFace API令牌（可选）


es:
  host: localhost
  port: 19200
  index_name: data_agent


llm:
  model_name: deepseek-v4-flash
  api_key: "sk-xxxxxx"
  base_url: https://api.deepseek.com
